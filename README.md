# RagAgents

# add local.settings.json file to the project root with the following content:

{
  "IsEncrypted": false,
  "Values": {
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "FUNCTIONS_WORKER_RUNTIME": "dotnet-isolated",
    "StorageConnectiontest": "DefaultEndpointsProtocol=https;ndpointSuffix=core.windows.net",

  }
}
